node_modules
dist
*.local
*.tsbuildinfo
.playwright-mcp
.DS_Store

# Eval build outputs — large, transient, regenerated per run (the harness in
# evals/ is committed; the per-iteration builds it grades are not).
harness-eval/

# Live runtime mirrors written by the viewer — regenerated each session.
.arta/runtime.json
.arta/feedback.json
.arta/snapshots/
.arta/viewer.log

# generated by evals/render.mjs (live-render check)
.arta/_render/
# playwright MCP scratch output
.playwright-mcp/
